// str.slice(3(incluso), 10(excluso)) - a string que começa no 3 ela é impressa e o 10 não
//Slice = Fatia

const cidade = "Brasília-DF"

let estado = cidade.slice(cidade.length - 2)

console.log(estado)